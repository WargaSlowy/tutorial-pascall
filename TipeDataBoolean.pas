program TipeDataBoolean;

var
    lulus: boolean;
    siap: boolean;
    lampu_hidup: boolean;

begin
    lulus := true;
    siap := false;

    lampu_hidup := true;

    writeln(lulus);
    writeln(siap);
    writeln(lampu_hidup);
end.